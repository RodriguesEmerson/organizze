'use client';
import { useState } from "react";

export function useSideBar(){
   const options = {
      2024: ['Novembro', 'Dezembro'],
      2025: ['Janeiro', 'Fevereiro'],  
   }
   const sideBarHander = {
      
   }
}